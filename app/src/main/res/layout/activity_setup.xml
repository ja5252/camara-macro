<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/black">

    <com.pepe.camaramacro.AutoFitTextureView
        android:id="@+id/texture"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="top"
        android:background="@color/panel"
        android:orientation="vertical"
        android:padding="16dp">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/setup_title"
            android:textColor="@color/white"
            android:textSize="20sp"
            android:textStyle="bold" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:text="@string/setup_hint"
            android:textColor="@color/white"
            android:textSize="13sp" />

        <!-- Aquí se escribe el veredicto de la lente ("da imagen" / "SIN IMAGEN" /
             "comprobando"), que es lo único que impide que el usuario elija el sensor de
             200 MP averiado y se quede con una cámara en negro: ocho debilidades del
             expediente (una crítica y siete altas) apuntan a esta línea de texto.
             El color era #FFD54F escrito a mano, un ámbar distinto del de la app que
             además no salía de ningún token: ahora es accent_bright, el mismo que usa el
             HUD para "atención aquí". accessibilityLiveRegion porque el veredicto CAMBIA
             solo bajo el usuario (el vigilante de 1,5 s lo reescribe sin que él toque
             nada) y sin región activa quien usa TalkBack no se entera de que la lente que
             está mirando acaba de declararse muerta. -->
        <TextView
            android:id="@+id/lbl_lens"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:accessibilityLiveRegion="polite"
            android:textColor="@color/accent_bright"
            android:textSize="15sp"
            android:textStyle="bold" />
    </LinearLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:background="@color/panel"
        android:gravity="center"
        android:orientation="horizontal"
        android:padding="12dp">

        <!-- minHeight explícito en los tres: Widget.Material3.Button mide 40dp de alto por
             defecto y ésta es la pantalla que decide si la app sirve para algo en este
             teléfono. El mismo objetivo táctil de 48dp que el propio proyecto ya exige en
             ProChip y en ModeTab. -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_prev"
            style="@style/Widget.Material3.Button.OutlinedButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:minHeight="@dimen/touch_min"
            android:text="@string/prev"
            android:textColor="@color/white" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_use"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:layout_marginEnd="8dp"
            android:layout_weight="1"
            android:minHeight="@dimen/touch_min"
            android:text="@string/use_this" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_next"
            style="@style/Widget.Material3.Button.OutlinedButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:minHeight="@dimen/touch_min"
            android:text="@string/next"
            android:textColor="@color/white" />
    </LinearLayout>
</FrameLayout>
