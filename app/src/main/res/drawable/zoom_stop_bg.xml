<?xml version="1.0" encoding="utf-8"?>
<!--
    Parada de zoom. La activa se distinguia SOLO por el color de la cifra: de reojo,
    mientras compones, no se ve cual esta puesta. Ahora la activa tiene ademas fondo
    ambar al 18% y filo ambar de 1,5dp.
-->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_selected="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/accent_soft" />
            <stroke android:width="1.5dp" android:color="@color/accent" />
            <corners android:radius="16dp" />
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/surface_panel" />
            <stroke android:width="1dp" android:color="@color/hairline" />
            <corners android:radius="16dp" />
        </shape>
    </item>
</selector>
